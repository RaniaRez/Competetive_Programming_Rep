class Solution:
    def firstUniqChar(self, s: str) -> int:
        freq=defaultdict(int)

        for char in s:
            freq[char]+=1
           

        for i in range(len(s)):
            char=s[i]
            if freq[char]==1:
                return i
