class Solution:
    def lengthOfLastWord(self, s: str) -> int:
        res = 0
        s = s.rstrip()
        for c in s:
            if c != ' ':
                res += 1
            else:
                res = 0
        return res
