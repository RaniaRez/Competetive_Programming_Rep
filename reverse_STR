class Solution:
    def reverseString(self, s: List[str]) -> None:
        """
        """
        offset=len(s)-1
        for i in range(len(s)//2):
            temp=s[i]
            s[i]=s[offset]
            s[offset]=temp
            offset-=1
